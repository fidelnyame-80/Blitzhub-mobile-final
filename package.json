{
  "name": "vercel-proxy",
  "version": "1.0.0",
  "description": "A proxy server deployed on Vercel to handle CORS.",
  "main": "api/proxy.js",
  "scripts": {
    "start": "node api/proxy.js"
  },
  "dependencies": {
    "node-fetch": "^3.3.1"  
  },
  "engines": {
    "node": ">=14"  
  }
}
